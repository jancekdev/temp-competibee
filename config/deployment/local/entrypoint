#!/bin/bash

set -o errexit
set -o pipefail
set -o nounset

# Wait for database to be ready
wait-for-it postgres:5432 --timeout=30 --strict -- echo "PostgreSQL is available"

# Run any initialization commands here if needed

exec "$@"
